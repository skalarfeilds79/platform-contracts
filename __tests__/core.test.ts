import { BigNumber } from 'ethers/utils';

import {
    CardsFactory, Cards
} from '../types'

import { ethers, Wallet } from 'ethers';

describe('Core', () => {

    let core: Cards;

    it('should run', () => {
        expect(true);
    })

});